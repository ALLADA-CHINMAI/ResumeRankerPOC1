# ─────────────────────────────────────────────────────────────────
# Copy this file to .env and fill in real values before running.
# ─────────────────────────────────────────────────────────────────

# Azure OpenAI
OPENAI_ENDPOINT=https://your-endpoint.openai.azure.com
OPENAI_DEPLOYMENT_NAME=gpt-4o
OPENAI_API_KEY=your-api-key-here
EMBEDDINGS_OPENAI_DEPLOYMENT_NAME=text-embedding-ada-002
OPENAI_API_VERSION=2025-01-01-preview

# Azure AD (only needed if your deployment uses token-based auth)
AUTH_TENANT_ID=your-tenant-id
AUTH_CLIENT_ID=your-client-id
AUTH_CLIENT_SECRET=your-client-secret
AUTH_SCOPE=api://your-scope/.default

# Azure Cognitive Search
AZURE_SEARCH_ENDPOINT=https://your-service.search.windows.net
AZURE_SEARCH_API_KEY=your-search-api-key

# Resume index — auto-created by the app on first run
AZURE_SEARCH_RESUME_INDEX_NAME=resume_chunks

# JD index — auto-created by the app on first run
AZURE_SEARCH_JD_INDEX_NAME=jd_chunks

# Azure Blob Storage
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=youraccount;AccountKey=yourkey==;EndpointSuffix=core.windows.net
RESUME_CONTAINER_NAME=resumes
JD_CONTAINER_NAME=jds

# ── LangGraph multi-agent pipeline ──────────────────────────────────────────
# Set to true to use the enhanced pipeline (parse_jd → skills_gap → score)
# Set to false (default) to use the original linear ranking.py pipeline
USE_LANGGRAPH=false

# Candidate data Excel — interview history for gap-aware ranking (USE_LANGGRAPH=true only)
# Two sheets: candidate_metadata, interview_history
# Leave blank to use candidate_data.xlsx in the project root (included as POC sample)
CANDIDATE_DATA_PATH=candidate_data.xlsx
